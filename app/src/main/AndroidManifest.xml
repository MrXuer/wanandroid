<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="us.xingkong.wanandroid">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/allTranslucent">
        <activity android:name=".MVP.view.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MVP.view.Homepage" />
        <activity
            android:name=".MVP.view.Search"
            android:theme="@style/halfTranslucent" />
        <activity android:name=".layout.Hotkey" />
        <activity android:name=".MVP.view.Register" />
        <activity android:name=".layout.NavigationHeader" />
        <activity android:name=".MVP.view.Collect" />
        <activity android:name=".layout.CardItem" />
        <activity android:name=".layout.Toolbar" />
        <activity android:name=".layout.Navigation" />
        <activity
            android:name=".layout.Webview"
            android:theme="@style/halfTranslucent" />
        <activity android:name=".layout.RecyclerHeader" />
        <activity android:name=".layout.RecyclerFooter" />
        <activity android:name=".layout.ProgressBar" />
        <activity android:name=".layout.RecyclerEmpty" />
        <activity android:name=".layout.RecyclerView" />
    </application>

</manifest>